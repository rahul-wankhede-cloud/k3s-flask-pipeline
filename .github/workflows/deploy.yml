name: Deploy to K3s
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted  # This tells GitHub to use local server!
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to K3s
        run: |
          kubectl apply -f deployment.yaml
          kubectl get pods